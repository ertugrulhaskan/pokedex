import React from "react";
// import PropTypes from 'prop-types'

const Filters = () => {
  return <div className="my-4">filters by pokemon type</div>;
};

// Filters.propTypes = {}

export default Filters;
